#!/bin/sh

set -e

echo "[ bin/psql ] Conecting to widgets_development"
PGPASSWORD=postgres psql -U postgres \
                         -h localhost \
                         -p 5432 \
                         widgets_development